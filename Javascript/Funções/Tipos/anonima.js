/* Exemplo de função anônima */

  /* Ao criar uma função anônima não é necessário informar o nome da mesma, como no caso abaixo quem assume o "nome" da função é a variável */
const soma = function (a, b){
  return a + b
}

soma(2,2) //retornará 4
soma(10,5) //retornará 15